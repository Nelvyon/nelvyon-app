# NELVYON-LABS — Integrados (estado real)

> Generado: 2026-07-15T18:30:58.232Z

## Ya integrados en NELVYON (runtime)

- **Apache Airflow** (`apache-airflow`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **Railway CLI** (`railway-cli`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **Ollama** (`ollama`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **llama.cpp** (`llama-cpp`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **LiteLLM** (`litellm`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **PostgreSQL** (`postgresql`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **pgvector** (`pgvector`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).
- **Sentence Transformers** (`sentence-transformers`) — Ya forma parte del stack NELVYON / Phase 2 (evidencia en catálogo).

**Total INTEGRADO\*:** 8

## Ganadores aprobados — cola de integración (NO en runtime aún)

Bloqueados por Fase 0 (certificación Router). Ver `NELVYON_LABS_WINNERS.md` y reservas:

- **Instructor** (`instructor`) → `Agentes` — MIT
- **GrowthBook** (`growthbook`) → `Analítica` — MIT
- **Chatwoot** (`chatwoot`) → `Atención al cliente` — MIT
- **OpenAI Whisper** (`whisper`) → `Audio / Voz` — MIT
- **BullMQ** (`bullmq`) → `Automatizaciones` — MIT
- **Strapi** (`strapi`) → `Contenido` — MIT
- **Atomic CRM** (`atomic-crm`) → `CRM` — MIT
- **Docker Compose** (`compose`) → `DevOps / CI` — Apache-2.0
- **Fontsource** (`fontsource`) → `Diseño` — MIT
- **Tesseract OCR** (`tesseract`) → `Documentos / OCR / PDF` — Apache-2.0
- **Suppression List Pattern** (`email-suppression-db`) → `Email` — Apache-2.0
- **Docker** (`docker`) → `Infraestructura` — Apache-2.0
- **FastAPI** (`fastapi`) → `Integraciones / APIs` — MIT
- **MCP TypeScript SDK** (`mcp-sdk-typescript`) → `MCP` — MIT
- **Mem0** (`mem0`) → `Memoria` — Apache-2.0
- **Playwright Browser Automation** (`playwright-browsers`) → `Navegación` — Apache-2.0
- **Uptime Kuma** (`uptime-kuma`) → `Observabilidad` — MIT
- **BookStack** (`bookstack`) → `Productividad` — MIT
- **Mattermost** (`mattermost`) → `Redes sociales` — MIT
- **Evidence** (`evidence-dev`) → `Reporting / BI` — MIT
- **TypeScript** (`typescript`) → `SaaS core` — Apache-2.0
- **Cheerio** (`cheerio`) → `Scraping` — MIT
- **Trivy** (`trivy`) → `Seguridad` — Apache-2.0
- **Umami** (`umami`) → `SEO` — MIT
- **Playwright** (`playwright`) → `Testing` — Apache-2.0
- **Tailwind CSS** (`tailwindcss`) → `UI / Paneles` — MIT
- **FFmpeg** (`ffmpeg`) → `Vídeo` — LGPL-3.0

## Regla

Ningún ganador nuevo se mergea a `nelvyon-app` hasta:
1. Soak Router 2h PASS + `completed=true`
2. Adaptador NELVYON + feature flag + tests
3. Auditoría licencia/seguridad del ganador concreto
