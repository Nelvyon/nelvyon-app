# frontend/ — archived

Legacy Vite shell superseded by **`apps/web`** (Next.js 15).

- Product surface: `https://nelvyon.com/saas/*`
- Do not add new features here; port to `apps/web` instead.
- Remaining pages use empty states (no `MOCK_*` seed data).
