# Example outputs (Phase B)

Static reference outputs. Run the simulator to regenerate under `../output/`:

```bash
pnpm -C apps/web exec tsx ../../backend/autonomous/scripts/run-simulation.ts all
```

Files:
- `landing-os-publish.example.json` — dry-run OsPublishPayload
- `chatbot-qa.example.json` — QaResult excerpt
- `seo-deliverables.example.json` — deliverables list
