{
 "_comentario": "Columnas que el codigo INSERTA y que la cadena oficial de migraciones NO crea. Medido contra una base VIRGEN reconstruida solo con migraciones. Trinquete: un hueco NUEVO hace fallar la prueba, y uno que ya no reproduce tambien —para que la lista no se convierta en algo que nadie lee. Los que faltaban por «falta una columna» los cierra la migracion 576; aqui solo quedan los que NO son eso, sino una decision: a que tabla pertenece cada servicio, y que semantica de tenencia tiene un workspace_id. Eso es alcance de producto.",
 "_medido": "2026-08-23",
 "huecos": [
  {
   "tabla": "chatbot_conversations",
   "columnas": [
    "visitor_info",
    "workspace_id"
   ],
   "origen": "backend/services/chatbot_service.py",
   "vivo": true,
   "en_produccion": false,
   "clase": "ROTO_HOY",
   "nota": "`workspace_id` en una tabla de inquilino no es un campo mas: decide el aislamiento y arrastra politica de RLS. Anadirla sin decidir su politica seria inventar semantica de tenencia."
  },
  {
   "tabla": "saas_tenants",
   "columnas": [
    "owner_user_id"
   ],
   "origen": "backend/saas/SaasSecurityEnterpriseService.ts",
   "vivo": true,
   "en_produccion": false,
   "clase": "ROTO_HOY",
   "nota": "`saas_tenants` ya tiene `user_id`. Si `owner_user_id` es un nombre distinto para lo mismo, la correccion es en el servicio, no una columna nueva: duplicar el dueno en dos columnas crea dos verdades."
  },
  {
   "tabla": "invoices",
   "columnas": [
    "client_address",
    "client_email",
    "client_name",
    "items",
    "logo_url",
    "tax_total",
    "user_id"
   ],
   "origen": "backend/saas/InvoicingService.ts",
   "vivo": false,
   "en_produccion": false,
   "clase": "CODIGO_MUERTO",
   "nota": "RESUELTO EL DIAGNOSTICO. La tabla `invoices` tiene DOS escritores y solo uno esta vivo: `SaasFacturasService`, al que llaman las rutas /api/saas/facturas*, y que usa la forma de inquilino (tenant_id, line_items) — la que la tabla tiene de verdad. `InvoicingService` escribe la forma de USUARIO (user_id, items, client_name...) y NADIE lo llama: solo lo referencian el barril backend/saas/index.ts y su propia prueba unitaria, que pasa porque usa un doble de base. Asi que el contrato de facturas ya esta definido y funciona: /api/saas/facturas -> SaasFacturasService -> invoices, y /api/saas/invoices -> SaasInvoiceService -> saas_invoices. Decision minima pendiente, y es UNA: borrar `InvoicingService` con su export del barril y su prueba, o conectarlo a algo. No lo borro yo."
  },
  {
   "tabla": "ab_tests",
   "columnas": [
    "duration_days",
    "metric",
    "updated_at",
    "user_id"
   ],
   "origen": "backend/saas/ABTestingService.ts",
   "vivo": false,
   "en_produccion": false,
   "clase": "CODIGO_MUERTO",
   "nota": "RESUELTO EL DIAGNOSTICO. El A/B vivo es /api/saas/ab-testing -> SaasAbTestingService -> ab_tests, y ESE si casa con la tabla (id, tenant_id, name, type, status, variants, winner_variant_id, confidence). `ABTestingService` escribe otra forma y NADIE lo llama: solo el barril y su prueba unitaria con doble de base. Decision minima pendiente, y es UNA: borrarlo con su export y su prueba, o conectarlo. No lo borro yo."
  }
 ],
 "_cerrados_por_la_576": [
  "affiliate_clicks",
  "campaigns",
  "retail_results",
  "saas_conversations",
  "security_events"
 ],
 "_nota_produccion": "La 576 esta escrita y certificada pero NO aplicada (ADR-064 = BLOCKED_ON_FOUNDER). Mientras no se aplique, esos cinco INSERT siguen fallando en produccion."
}