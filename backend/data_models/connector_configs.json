{
    "title": "connector_configs",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "User ID"
        },
        "connector_name": {
            "type": "string",
            "description": "Connector name: google_ads, meta_ads, whatsapp, mailchimp, zapier, hubspot, slack, shopify, github, stripe, google_analytics, sendgrid"
        },
        "display_name": {
            "type": "string",
            "description": "Display name"
        },
        "status": {
            "type": "string",
            "description": "Status: connected, disconnected, error"
        },
        "config_json": {
            "type": "string",
            "description": "JSON configuration"
        },
        "last_sync_at": {
            "type": "string",
            "format": "date-time",
            "description": "Last sync timestamp"
        },
        "sync_status": {
            "type": "string",
            "description": "Sync status: synced, syncing, error"
        },
        "events_count": {
            "type": "integer",
            "description": "Total events processed"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "Created timestamp"
        }
    },
    "required": [
        "id",
        "user_id",
        "connector_name"
    ]
}