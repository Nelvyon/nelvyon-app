[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# alembic upgrade head runs inside CMD before uvicorn starts (see Dockerfile).
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
