# --- Node / pnpm / monorepo ---
node_modules/
.pnpm-store/
**/node_modules/

# Lockfile alternativo si solo usas pnpm (evita commits duplicados)
package-lock.json

# Lock único del workspace en la raíz (evita divergencia con npm/pnpm mezclados)
frontend/pnpm-lock.yaml

# --- Python ---
.venv/
venv/
ENV/
env/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
pip-wheel-metadata/

# --- Reference templates (local extract only; never ship) ---
.reference/

# AIOR branded static site — generado por brand+content+fidelity; se versiona el pack
# (sin vídeos pesados duplicados). Regenerar: ver docs/ops/AIOR_NELVYON_TRANSFORM_STATUS.md
# apps/web/public/www/

# Portable tooling (ffmpeg essentials, etc.)
.tools/

# Raw screen-recording frames (keep only /clean curated)
scripts/data/aior-video-frames/v*.png

# Heavy AIOR demo videos (optional local; pack HTML funciona sin ellos)
# Permitir vídeo hero AIOR en /www (necesario para fidelidad visual)
# apps/web/public/www/**/*.mp4

# --- Local audit / scratch outputs ---
tmp-*.txt
tmp-*.json
vitest-out.txt
.tmp-*.txt

# --- Build / artefactos ---
dist/
build/
out/
.next/
.source/
.nuxt/
.turbo/
.nx/
.cache/
*.tsbuildinfo
/.build-out.txt
/.vitest-out.txt
/.tsc-out.txt
/.smokes-out.txt
coverage/
htmlcov/
.coverage
coverage.xml
*.cover

# --- Tests / calidad ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# --- Entorno y secretos (nunca al repo) ---
.env
.env.local
.env.*.local
.env.*
!.env.example
!.env*.example
secrets.*
!backend/core/secrets.py
*.pem
*.key
*.p12

# --- Datos locales / logs ---
*.log
logs/
*.db
*.sqlite3

# --- Mobile / Android (SDK paths must never commit) ---
apps/mobile/android/local.properties
**/local.properties

# --- IDE / OS ---
.idea/
.vscode/*.local.json
.DS_Store
Thumbs.db
*.swp
*.swo

# --- E2E / Playwright ---
playwright-report/
playwright-report-saas/
test-results/
blob-report/

# --- Misc ---
*.local
.turbo/cache
.release-logs/

# --- Artefactos locales (no subir al repo) ---
nelvyon_backend.zip
tools/
# Track intentional MCP / Private AI tool sources (negates broad tools/ above)
!backend/private-ai/tools/
!backend/private-ai/tools/**
!backend/mcp/tools/
!backend/mcp/tools/**
backend/autonomous/output/

# Accidental local-ai copy under Next app (canonical: backend/local-ai/)
apps/web/backend/

# Envato Elements seeds — ZIPs never committed, only JSON metadata
backend/data/envato-seeds/**/*.zip
backend/data/envato-seeds-metadata.json
backend/data/envato-seeds-metadata.json.*
backend/data/sector-template-catalog.json
backend/data/*.bak
backend/os-agents/seeds/download-envato-*.ts
backend/os-agents/seeds/run-envato-*.ts
backend/os-agents/seeds/envato-metadata-io.ts
backend/os-agents/seeds/os-service-template-catalog.ts
backend/os-agents/seeds/sector-template-catalog.ts
backend/data/voice_pilot*/
*.bat
.claude/agents/
.envato-browser-profile/
.edge-envato-download-profile*/
.tmp-local-ai-db-url.txt

# Raw SaaS marketing PNGs (regen via Playwright; keep WebP only)
apps/web/public/brand/public/saas-shots/raw/