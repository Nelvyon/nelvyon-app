import { OsPlatformDashboard } from "@/features/os-shell/components/OsPlatformDashboard";

export const metadata = {
  title: "NELVYON OS — Dashboard",
  description: "Centro operativo interno NELVYON",
};

export default function OsDashboardPage() {
  return <OsPlatformDashboard />;
}
