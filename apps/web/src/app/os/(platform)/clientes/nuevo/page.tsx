import { OsClientCreateView } from "@/features/os-shell/clients/OsClientCreateView";

export const metadata = {
  title: "Nuevo cliente · NELVYON OS",
};

export default function OsClienteNuevoPage() {
  return <OsClientCreateView />;
}
