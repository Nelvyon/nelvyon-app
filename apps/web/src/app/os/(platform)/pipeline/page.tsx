import { OsPipelineView } from "@/features/os-shell/pipeline/OsPipelineView";

export const metadata = {
  title: "Pipeline · NELVYON OS",
};

export default function OsPipelinePage() {
  return <OsPipelineView />;
}
