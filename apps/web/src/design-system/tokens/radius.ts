/**
 * Corner radii — aligned with `globals.css` CSS variables consumed by Tailwind `rounded-*`.
 */

export const nelvyonRadiusPx = {
  sm: 6,
  md: 8,
  lg: 10,
  xl: 12,
} as const;

export const nelvyonRadius = {
  sm: "0.375rem",
  md: "0.5rem",
  lg: "0.625rem",
  xl: "0.75rem",
} as const;
